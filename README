NAME
        pirate - command line 'arrrrg processor

DESCRIPTION


USAGE


BUILD
        To build from source:
            $ git clone git@github.com:markhibberd/pirate.git
            $ cd pirate
            $ make

        Artifacts will be built into:
            <repository>/gen/dist/.


TODO
        . Proguard
        . Flags types, mandatory arguments, optional arguments, formats -xfoo -x foo -x=foo
        . Sub commands
        . Pretty up usage output
        . API tidy
        . Demos
        . Tests
