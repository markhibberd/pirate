package io.mth.pirate

import org.scalacheck.Prop._
import org.scalacheck.Properties

object FlagTest extends Properties("Flag") {
  property("place holder -- test me you dodgy bastard") =
       forAll((s: String) =>
            true)
}